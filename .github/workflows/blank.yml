name: Build Python for Windows ARM64
on: [push, pull_request]

jobs:
  build:
    runs-on: windows-latest
    strategy:
      matrix:
        pyver: ['3.9.0rc2']

    steps:
      - name: Compile Python
        run: |
          curl -L -s https://github.com/python/cpython/archive/v${pyver}.tar.gz -o python.tar.gz
          tar -xzvf python.tar.gz
          cd "cpython-${pyver}"
          PCbuild\build.bat -e -p ARM64
